<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FamilyManager.Views.LoginPage"
             NavigationPage.HasNavigationBar="False">

    <ContentPage.Resources>
        <Color x:Key="PrimaryColor">#667eea</Color>
        <Color x:Key="SecondaryColor">#764ba2</Color>
    </ContentPage.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource PrimaryColor}" Offset="0.0" />
                <GradientStop Color="{StaticResource SecondaryColor}" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Border Stroke="#80FFFFFF"
                StrokeThickness="1"
                BackgroundColor="White"
                Padding="30,40"
                Margin="20"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="350">

            <Border.StrokeShape>
                <RoundRectangle CornerRadius="30" />
            </Border.StrokeShape>

            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,10" Radius="30" Opacity="0.15" />
            </Border.Shadow>

            <VerticalStackLayout Spacing="20">

                <Label Text="Chào mừng bạn"
                       TextColor="{StaticResource SecondaryColor}"
                       FontSize="28"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       Margin="0,0,0,10" />

                <Border StrokeThickness="1" Stroke="#E0E0E0" BackgroundColor="#F8F9FA" HeightRequest="50" Padding="15,0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="12" />
                    </Border.StrokeShape>
                    <Grid ColumnDefinitions="30, *">
                        <Label Text="📧" VerticalOptions="Center" HorizontalOptions="Center"/>
                        <Entry Grid.Column="1"
                               Text="{Binding Email}"
                               Placeholder="Nhập Email"
                               Keyboard="Email"
                               TextColor="#333"
                               PlaceholderColor="#999"
                               BackgroundColor="Transparent"
                               Margin="10,0,0,0"/>
                    </Grid>
                </Border>

                <Border StrokeThickness="1" Stroke="#E0E0E0" BackgroundColor="#F8F9FA" HeightRequest="50" Padding="15,0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="12" />
                    </Border.StrokeShape>
                    <Grid ColumnDefinitions="30, *">
                        <Label Text="🔒" VerticalOptions="Center" HorizontalOptions="Center"/>
                        <Entry Grid.Column="1"
                               Text="{Binding Password}"
                               IsPassword="True"
                               Placeholder="Nhập mật khẩu"
                               TextColor="#333"
                               PlaceholderColor="#999"
                               BackgroundColor="Transparent"
                               Margin="10,0,0,0"/>
                    </Grid>
                </Border>

                <Button Text="ĐĂNG NHẬP"
                        Command="{Binding LoginCommand}"
                        TextColor="White"
                        FontAttributes="Bold"
                        CornerRadius="25"
                        HeightRequest="50"
                        Margin="0,15,0,0">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0.0"/>
                            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Button.Background>
                    <Button.Shadow>
                        <Shadow Brush="{StaticResource PrimaryColor}" Offset="0,4" Radius="10" Opacity="0.4"/>
                    </Button.Shadow>
                </Button>

                <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,10,0,0">
                    <Label Text="Chưa có tài khoản?" TextColor="#666" FontSize="14"/>
                    <Label Text="Đăng ký ngay" 
                           TextColor="{StaticResource SecondaryColor}" 
                           FontAttributes="Bold" 
                           FontSize="14"
                           TextDecorations="Underline">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToRegisterCommand}"/>
                        </Label.GestureRecognizers>
                    </Label>
                </HorizontalStackLayout>

            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>