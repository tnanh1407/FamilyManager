<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:FamilyManager.ViewModels"
             x:Class="FamilyManager.Views.RegisterPage"
             NavigationPage.HasNavigationBar="False">

    <!-- VIEWMODEL -->
    <ContentPage.BindingContext>
        <viewModels:RegisterViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <Color x:Key="PrimaryColor">#667eea</Color>
        <Color x:Key="SecondaryColor">#764ba2</Color>
    </ContentPage.Resources>

    <Grid>
        <!-- BACKGROUND -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollView>
            <Border BackgroundColor="White"
                    Padding="30,35"
                    Margin="20"
                    WidthRequest="350"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="30"/>
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="16">

                    <Label Text="Tạo tài khoản"
                           FontSize="26"
                           FontAttributes="Bold"
                           TextColor="{StaticResource SecondaryColor}"
                           HorizontalOptions="Center"/>

                    <!-- FULL NAME -->
                    <Border BackgroundColor="#F8F9FA" HeightRequest="50" Padding="15,0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Grid ColumnDefinitions="30,*">
                            <Label Text="👤" VerticalOptions="Center"/>
                            <Entry Grid.Column="1"
                                   Placeholder="Họ và tên"
                                   Text="{Binding FullName}"
                                   BackgroundColor="Transparent"/>
                        </Grid>
                    </Border>

                    <!-- USERNAME -->
                    <Border BackgroundColor="#F8F9FA" HeightRequest="50" Padding="15,0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Grid ColumnDefinitions="30,*">
                            <Label Text="🏷️" VerticalOptions="Center"/>
                            <Entry Grid.Column="1"
                                   Placeholder="Username"
                                   Text="{Binding Username}"
                                   BackgroundColor="Transparent"/>
                        </Grid>
                    </Border>

                    <!-- EMAIL -->
                    <Border BackgroundColor="#F8F9FA" HeightRequest="50" Padding="15,0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Grid ColumnDefinitions="30,*">
                            <Label Text="📧" VerticalOptions="Center"/>
                            <Entry Grid.Column="1"
                                   Placeholder="Email"
                                   Keyboard="Email"
                                   Text="{Binding Email}"
                                   BackgroundColor="Transparent"/>
                        </Grid>
                    </Border>

                    <!-- PASSWORD -->
                    <Border BackgroundColor="#F8F9FA" HeightRequest="50" Padding="15,0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Grid ColumnDefinitions="30,*">
                            <Label Text="🔒" VerticalOptions="Center"/>
                            <Entry Grid.Column="1"
                                   Placeholder="Mật khẩu"
                                   IsPassword="True"
                                   Text="{Binding Password}"
                                   BackgroundColor="Transparent"/>
                        </Grid>
                    </Border>

                    <!-- CONFIRM -->
                    <Border BackgroundColor="#F8F9FA" HeightRequest="50" Padding="15,0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Grid ColumnDefinitions="30,*">
                            <Label Text="🔐" VerticalOptions="Center"/>
                            <Entry Grid.Column="1"
                                   Placeholder="Xác nhận mật khẩu"
                                   IsPassword="True"
                                   Text="{Binding ConfirmPassword}"
                                   BackgroundColor="Transparent"/>
                        </Grid>
                    </Border>

                    <!-- REGISTER BUTTON -->
                    <Button Text="ĐĂNG KÝ"
                            Command="{Binding RegisterCommand}"
                            HeightRequest="50"
                            FontAttributes="Bold"
                            TextColor="White"
                            CornerRadius="25">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>

                    <!-- BACK TO LOGIN -->
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="5">
                        <Label Text="Đã có tài khoản?" TextColor="#666"/>
                        <Label Text="Đăng nhập"
                               TextColor="{StaticResource SecondaryColor}"
                               FontAttributes="Bold"
                               TextDecorations="Underline">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateBackCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>

                </VerticalStackLayout>
            </Border>
        </ScrollView>
    </Grid>
</ContentPage>
