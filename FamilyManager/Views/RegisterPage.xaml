<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MovieTicketApp.Views.RegisterPage"
             NavigationPage.HasNavigationBar="False">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#667eea" Offset="0.0" />
                <GradientStop Color="#764ba2" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollView VerticalScrollBarVisibility="Never">

            <VerticalStackLayout VerticalOptions="Center" 
                                 Padding="0,40,0,40">
                <Border Stroke="#80FFFFFF"
                        StrokeThickness="1.5"
                        BackgroundColor="#20FFFFFF"
                        Padding="30"
                        Margin="20,0"
                        HorizontalOptions="Center"
                        WidthRequest="350">

                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="30" />
                    </Border.StrokeShape>

                    <Border.Shadow>
                        <Shadow Brush="Black" Offset="0,10" Radius="20" Opacity="0.2" />
                    </Border.Shadow>

                    <VerticalStackLayout Spacing="15">

                        <Label Text="Tạo Tài Khoản"
                               TextColor="White"
                               FontSize="26"
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               Margin="0,0,0,10" />

                        <Border StrokeThickness="0" BackgroundColor="#40FFFFFF" HeightRequest="50" Padding="15,0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="25" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="30, *">
                                <Label Text="🏷️" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center"/>
                                <Entry Grid.Column="1"
                                       Placeholder="Họ và tên"
                                       Text="{Binding FullName}"
                                       PlaceholderColor="#D0D0D0"
                                       TextColor="White"
                                       BackgroundColor="Transparent"
                                       Margin="5,0,0,0"/>
                            </Grid>
                        </Border>

                        <Border StrokeThickness="0" BackgroundColor="#40FFFFFF" HeightRequest="50" Padding="15,0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="25" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="30, *">
                                <Label Text="📧" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center"/>
                                <Entry Grid.Column="1"
                                       Placeholder="Email"
                                       Text="{Binding Email}"
                                       Keyboard="Email" 
                                       PlaceholderColor="#D0D0D0"
                                       TextColor="White"
                                       BackgroundColor="Transparent"
                                       Margin="5,0,0,0"/>
                            </Grid>
                        </Border>

                        <Border StrokeThickness="0" BackgroundColor="#40FFFFFF" HeightRequest="50" Padding="15,0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="25" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="30, *">
                                <Label Text="🔒" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center"/>
                                <Entry Grid.Column="1"
                                       Placeholder="Mật khẩu"
                                       Text="{Binding Password}"
                                       IsPassword="True"
                                       PlaceholderColor="#D0D0D0"
                                       TextColor="White"
                                       BackgroundColor="Transparent"
                                       Margin="5,0,0,0"/>
                            </Grid>
                        </Border>

                        <Border StrokeThickness="0" BackgroundColor="#40FFFFFF" HeightRequest="50" Padding="15,0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="25" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="30, *">
                                <Label Text="🔐" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center"/>
                                <Entry Grid.Column="1"
                                       Placeholder="Xác nhận mật khẩu"
                                       Text="{Binding ConfirmPassword}"
                                       IsPassword="True"
                                       PlaceholderColor="#D0D0D0"
                                       TextColor="White"
                                       BackgroundColor="Transparent"
                                       Margin="5,0,0,0"/>
                            </Grid>
                        </Border>

                        <Button Text="ĐĂNG KÝ"
                                Command="{Binding RegisterCommand}"
                                TextColor="#764ba2"
                                BackgroundColor="White"
                                FontAttributes="Bold"
                                CornerRadius="25"
                                HeightRequest="50"
                                Margin="0,15,0,0">
                            <Button.Shadow>
                                <Shadow Brush="Black" Offset="0,4" Radius="10" Opacity="0.2"/>
                            </Button.Shadow>
                        </Button>

                        <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,10,0,0">
                            <Label Text="Đã có tài khoản?" TextColor="White" FontSize="13"/>
                            <Label Text="Đăng nhập" 
                                   TextColor="White" 
                                   FontAttributes="Bold" 
                                   FontSize="13"
                                   TextDecorations="Underline">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding NavigateBackCommand}"/>
                                </Label.GestureRecognizers>
                            </Label>
                        </HorizontalStackLayout>

                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>